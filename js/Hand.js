/**
 * Class Hand
 */

export class Hand {
    constructor(i, gameDeck) {
        /**
         * @param {number} i
         * @param {array} gameDeck
         */

        this.gameDeck = gameDeck

        let hand = this.gameDeck[i]

        return (hand)
    }
}